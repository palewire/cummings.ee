{
  "private": true,
  "scripts": {
    "build": "NODE_ENV=production bake build --config",
    "git-pre-commit": "lint-staged",
    "start": "NODE_ENV=development bake serve --config"
  },
  "dependencies": {
    "@datagraphics/baker": "file:baker",
    "@datagraphics/delivery": "^0.6.0",
    "archieml": "^0.4.2",
    "intersection-observer": "^0.12.0",
    "json-2-csv": "^3.10.3",
    "rfs": "^9.0.3",
    "sass-mq": "^5.0.1"
  },
  "devDependencies": {
    "@datagraphics/prettier-config": "^2.0.0",
    "@zeit/git-hooks": "^0.1.4",
    "lint-staged": "^10.5.4",
    "prettier": "^2.2.1"
  },
  "prettier": "@datagraphics/prettier-config",
  "lint-staged": {
    "*.{js,md,scss}": [
      "prettier --write"
    ]
  }
}
